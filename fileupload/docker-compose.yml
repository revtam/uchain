version: "3.9"

services:
  fileserver:
    env_file:
      - .env
    build:
      context: .
      dockerfile: Dockerfile
    command:
      fileserver.js
    ports:
      - "${PORT}:${PORT}"
    volumes:
      - filestorage:/app/files

volumes:
  filestorage: